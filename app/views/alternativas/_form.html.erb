<div class="row">
  <div class="col-md-5">
<%= form_for(alternativa) do |f| %>
  <% if alternativa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alternativa.errors.count, "error") %> prohibited this alternativa from being saved:</h2>

      <ul>
      <% alternativa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :projeto_id, value: params[:projeto_id] %>
  <div class="form-group">
    <%= f.label :nome %>
    <%= f.text_field :nome, :class => "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :codigo %>
    <%= f.text_field :codigo, :class => "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit "Salvar", :class => "btn btn-primary" %>
    <%= link_to "Cancelar",Projeto.find(params[:projeto_id]) ,:class => "btn btn-danger"%>
  </div>
<% end %>
</div>
</div>
