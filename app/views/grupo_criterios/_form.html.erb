<%= form_for(grupo_criterio) do |f| %>
  <% if grupo_criterio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grupo_criterio.errors.count, "error") %> prohibited this grupo_criterio from being saved:</h2>

      <ul>
      <% grupo_criterio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :peso %>
    <%= f.text_field :peso %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
