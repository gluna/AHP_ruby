<%= form_for(julgamento) do |f| %>
  <% if julgamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(julgamento.errors.count, "error") %> prohibited this julgamento from being saved:</h2>

      <ul>
      <% julgamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :usuario_id %>
    <%= f.text_field :usuario_id %>
  </div>

  <div class="field">
    <%= f.label :projeto_id %>
    <%= f.text_field :projeto_id %>
  </div>

  <div class="field">
    <%= f.label :criterio %>
    <%= f.text_field :criterio %>
  </div>

  <div class="field">
    <%= f.label :alternativa_id %>
    <%= f.text_field :alternativa_id %>
  </div>

  <div class="field">
    <%= f.label :alternativa_id %>
    <%= f.text_field :alternativa_id %>
  </div>

  <div class="field">
    <%= f.label :valor %>
    <%= f.text_field :valor %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
