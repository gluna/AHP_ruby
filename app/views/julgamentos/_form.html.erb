<%= form_for(julgamento) do |f| %>
  <% if julgamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(julgamento.errors.count, "error") %> prohibited this julgamento from being saved:</h2>

      <ul>
      <% julgamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!--<div class="field">
    <%= f.label :usuario_id %>
    <%= f.text_field :usuario_id %>
  </div>--><%= f.hidden_field :usuario_id, value: current_usuario.id %>

  <!--<div class="field">
    <%= f.label :projeto_id %>
    <%= f.text_field :projeto_id %>
  </div>--><%= f.hidden_field :projeto_id, value: params[:projeto_id] %>

  <% @julgamentos = Julgamento.find(:all, :usuario_id => current_usuario.id) %>

  <% Criterio.all.each do |c|  %>
    <h2><%= c.nome %></h2>
    <table>
      <% Alternativa.all.each do |a|%>
        <h3><%= a.nome %></h3>
      <% end %>
    </table>
    </br>
  <% end %>

  <!--<div class="field">
    <%= f.label :criterio_id %>
    <%= f.text_field :criterio_id %>
  </div>

  <div class="field">
    <%= f.label :alternativa_1_id %>
    <%= f.text_field :alternativa_1_id %>
  </div>

  <div class="field">
    <%= f.label :alternativa_2_id %>
    <%= f.text_field :alternativa_2_id %>
  </div>

  <div class="field">
    <%= f.label :valor %>
    <%= f.text_field :valor %>
  </div>-->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
